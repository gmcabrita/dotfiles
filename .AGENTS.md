I am `gmcabrita` aka Gonçalo Mendes Cabrita on GitHub and other places.

Be extremely concise. Prefer fragments over prose, but keep status, risks, and next steps clear.

## Code exploration

You run in an environment where ast-grep (`ast-grep`) is available; whenever a search requires syntax aware or structural matching, default to `ast-grep --lang $language -p '<pattern>'` and avoid falling back to text-only tools like `rg` or `grep` unless I explicitly request a plain-text search. Use `ast-grep outline` as a cheap first pass over unfamiliar code before reading full files. It extracts file/module structure through ast-grep/tree-sitter and returns precise ranges for imports, exports, top-level symbols, and direct members.

When using the Go programming language:

- For finding APIs or callers use `gopls`
- For browsing documentation use `go doc`

## Code Quality Standards

- Make minimal, surgical changes
- Design for testability using "functional core, imperative shell": keep pure business logic separate from code that does IO
- **Make illegal states unrepresentable**: Model domain with ADTs/discriminated unions; parse inputs at boundaries into typed structures; if state can't exist, code can't mishandle it
- **Abstractions**: Consciously constrained, pragmatically parameterised, doggedly documented
- Use hard cutover; no backward compatibility unless asked. If public API/data migration/user-facing breakage is involved, flag impact first

### **ENTROPY REMINDER**

Fight entropy: no hacks, no shortcuts, leave code clearer than found.

## Testing

- Write tests that verify semantically correct behavior
- **Failing tests are acceptable** when they expose genuine bugs and test correct behavior

## Comments

Write comments assuming the reader is new to the codebase, but familiar with the goal of the project.

## SCM, Git, Pull Requests, Commits

- **Never** add Codex/Claude or similar to attribution or as a contributor PRs, commits, messages, or PR descriptions
- **gh CLI available** for GitHub operations (PRs, issues, etc.)
- **Never** submit reviews/comments to PRs unless I confirm so
- Use an independent code reviewer, e.g.
  - If you are using gpt-5.6-sol for the primary code review, pick anthropic/claude-fable-5 for the secondary code review
  - If you implemented something using gpt-5.6-sol and want to run a review step, use anthropic/claude-fable-5 for a final review of your work
